@props(['features'])

<section>

    <div class="grid grid-cols-3">

        @foreach($features as $feature)
            <div class="">

            </div>
        @endforeach

    </div>

</section>
